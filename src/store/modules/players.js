const state = {
  all: [
    { name: 'Spooky Steve' }
  ]
}

const getters = {
  players: state => state.all
}

const actions = {
}

const mutations = {
}

export default {
  state,
  getters,
  actions,
  mutations
}
